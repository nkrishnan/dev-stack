title = "gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Allow example/template model names in config files"
paths = [
  "templates/llm-app/openrouter-models.json",
  "opencode/opencode.json",
  "templates/.*",
]
